spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/delivery_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update # Cria as tabelas automaticamente (similar ao prisma db push para dev)
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: "sua_chave_secreta_super_segura_com_pelo_menos_32_caracteres_para_o_hs256"
  expiration: 86400000 # 1 dia em milissegundos